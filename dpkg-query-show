dpkg-query --show --showformat '${package}\tVersion:${version}\tArchitecture:${architecture}\tPriority:${priority}\tMaintainer:${maintainer}\tHomepage:${homepage}\tOrigin:${origin}\tInstalled-Size:${installed-size}\tPre-Depends:${pre-depends}\tDepends:${depends}\tRecommends:${recommends}\tSuggests:${suggests}\tProvides:${provides}\tReplaces:${replaces}\tEnhances:${enhances}\tConflicts:${conflicts}\tBreaks:${breaks}\tDescription: ${description}' $@ 
